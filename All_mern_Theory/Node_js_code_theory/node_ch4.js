//PATH Module:-The path module provides utilities for working with file and directory paths it can be accessed using.

const path = require('path');

console.log(path.basename(__filename))
//console.log(__filename);