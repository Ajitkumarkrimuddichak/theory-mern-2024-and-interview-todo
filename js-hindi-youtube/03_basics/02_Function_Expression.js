/* What is Function Expression :- When we create a function and assign to a 
Variable known as function Expression.

Note :- Before  Calling is note Possible &  Only for After calling Possible.
*/

// Ex:-
// let AssignVariable = function show(a, b) {
//   const c = a + b;
//   console.log(`Total sum of = ${c}`);
// };
// AssignVariable(10, 20);

//(or)
const funAssigneVariable = function () {
  console.log("Hello");
};
funAssigneVariable();
